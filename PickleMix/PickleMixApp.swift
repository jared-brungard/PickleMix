
import SwiftUI
// Loads the Format SelectionView as the initial view when the app is loaded
@main
struct PickleMixApp: App {
    var body: some Scene {
        WindowGroup {
            FormatSelectionView()
        }
    }
}
